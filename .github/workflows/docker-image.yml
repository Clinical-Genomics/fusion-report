name: Docker Image CI [mittelman_fix]

on:
  push:
    branches: [ mittelman_fix ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build Docker image
      run: |
        docker build . --file Dockerfile --tag 2.1.5patched:$(date +%s)
